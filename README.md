# neighbors-cp-graph-gen
A constraint-based graph generator that produces regular, non-isomorphic graphs using a Neighbors Constraint Programming (NCP) model. Each vertex's neighborhood is constrained programmatically to enforce regularity while systematically eliminating isomorphic duplicates.
